# README

## This application uses the subdomain to handle the `locale`.
## You must add the following lines to your `/etc/hosts` file :
- 127.0.0.1 en.application.local
- 127.0.0.1 fr.application.local
#### Then visit `locale`.application.local:3000 to have a localized version of the app.
