= simple_form_for [:admin, @article] do |f|
  = f.input :title
  = f.input :content, as: :text
  = f.input :image
  = f.input :user_id, collection: User.pluck(:email, :id), prompt: 'Choose a user'
  %div{style: 'margin-top: 20px'}
    = f.submit 'Save article'
