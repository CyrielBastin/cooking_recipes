Those validators are here to emulate those in the backend.
That way, we don't have to make unnecessary calls to to backend.

> [!WARNING]
> Don't modify those validators without checking the backend first.
